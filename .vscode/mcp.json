{
  "inputs": [
    {
      "id": "hostinger_api_token",
      "type": "promptString",
      "description": "Hostinger API token",
      "password": true
    }
  ],
  "servers": {
    "hostinger-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": ["hostinger-api-mcp@latest"],
      "env": {
        "API_TOKEN": "${input:hostinger_api_token}"
      }
    }
  }
}
